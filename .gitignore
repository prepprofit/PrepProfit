# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# env files (NEVER commit secrets)
.env
.env.local
.env.*.local

# drizzle
/drizzle/meta/_journal.json.bak

# misc
*.tsbuildinfo
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea

# the original base zip (kept locally, not versioned)
Wibox-dashboard-app-main.zip

# source logo (canonical copy lives in public/logo_final.jpg)
/logo_final.jpg

# agent skills (tooling assets — reproducible via `npx skills add`)
/.agents/
/.claude/
/skills-lock.json
