# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# playwright (Sprint 5b)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# env files (NEVER commit secrets)
.env
.env.local
.env.*.local

# drizzle
/drizzle/meta/_journal.json.bak

# misc
*.tsbuildinfo
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea

# the original base zip (kept locally, not versioned)
Wibox-dashboard-app-main.zip

# source brand assets (canonical copies live in public/ and app/)
/logo/
/logo_final.jpg

# graphify knowledge-graph artifacts (regenerate with `/graphify .`)
/graphify-out/

# agent skills (tooling assets — reproducible via `npx skills add`)
/.agents/
/.claude/
/skills-lock.json
.vercel
.env.prod.tmp
.vercel-prod.env.tmp
scripts/test-email.mjs

# clerk configuration (can include secrets)
/.clerk/
